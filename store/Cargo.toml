[package]
name = "store"
version = "0.1.0"
edition = "2024"

[dependencies]
base = { path = "../base" }
runtime = { path = "../runtime" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rusqlite = { version = "0.39", features = ["bundled"] }
dirs = "6.0.0"
